@require './config';
@require './nprogress';
@require './code';
@require './custom-blocks';
@require './arrow';
@require './wrapper';
@require './toc';

html, body {
  padding: 0;
  margin: 0;
}

body {
  font-family: Arial;
  // -webkit-font-smoothing antialiased
  // -moz-osx-font-smoothing grayscale
  font-size: 16px;
  color: $textColor;
}

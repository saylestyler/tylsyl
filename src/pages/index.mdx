import { graphql } from 'gatsby'
import Link from '@components/Link.js'
import BlogList from '@components/BlogList.js'

# Hi 👋

Welcome to Tyler Sayles personal dev portal / brain dump / link aggregator. I'm a software engineer in NYC. I've worked for [VFILES](https://instagram.com/vfiles) as a full-stack + iOS engineer and [@Co-StarAstrology](https://www.costarastrology.com) as a consultant. 


Development wise I'm interested in build automation + bundlers, com/trans/pre/optimizing -compilers, and [shell scripting](https://github.com/saylestyler/totfiles). Non-development wise I like Fiction (Gaddis, Pynchon, Woolf) and... more reading.

## Neo-Posts

<BlogList
  nodes={props.data.allMdx.edges.filter(
    ({ node }) => !node.fields.slug.includes('legacy')
  )}
/>

## Elderly Posts

<BlogList
  nodes={props.data.allMdx.edges.filter(({ node }) =>
    node.fields.slug.includes('legacy')
  )}
/>

export const pageQuery = graphql`
  query blogIndex {
    allMdx {
      edges {
        node {
          id
          frontmatter {
            title
            publishDate
          }
          fields {
            slug
          }
        }
      }
    }
  }
`

---

This site is built using a lot of <Link to="/credits">cool open source
libraries</Link> and another, cooler OSS <Link to="github.com/matthamlin">blog</Link>
